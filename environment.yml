name: heart_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - streamlit
      - pandas
      - numpy
      - scikit-learn
      - matplotlib
      - seaborn
      - joblib
      - experta
      - nbformat
